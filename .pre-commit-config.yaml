default_stages: [push]
repos:
-   repo: https://github.com/pre-commit/mirrors-pylint
    rev: v2.3.1
    hooks:
    -   id: pylint
        files: $GIT_DIR/../libraries
        args:
        -   --rcfile=$GIT_DIR/../.pylintrc
        always_run: true
        verbose: true